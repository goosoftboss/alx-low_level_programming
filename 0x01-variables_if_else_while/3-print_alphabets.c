#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 'A'; n <= 'z'; n++)
	{
		if (n >= 'Z')
		{
		putchar(n);
		}
		else
		{
		putchar(n);
		}
	}
	putchar('\n');
	return (0);
}
